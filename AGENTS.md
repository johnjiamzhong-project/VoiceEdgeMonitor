# VoiceEdgeMonitor Agent 开发规则

## 适用范围

本文件适用于本仓库及其子目录。用户的明确要求优先于本文件；本文件约束项目实现方式，`README.md` 负责描述项目目标、架构和验收要求。

开始修改前，必须先阅读 `README.md` 和本文件。不要把尚未实现的规划描述成已经完成的功能。

## 项目边界

VoiceEdgeMonitor 由两部分组成：

- RK3588 板端：音频采集、VAD、离线 ASR、状态管理。
- 主机端：接收板端数据并提供 Web 监控、波形、识别结果和事件展示。

Web 和网络传输属于监控旁路，不能反向成为板端采集和识别的依赖。

## 不可违反的工程约束

### 板端主链路优先

- 板端音频采集、VAD 和 ASR 必须在主机断开、浏览器关闭或 Web 服务重启时继续运行。
- 网络发送、Web 页面卡顿和客户端处理速度不得阻塞音频采集或 ASR。
- 不得为了满足 Web 展示而牺牲板端主链路的连续性。

### 音频与网络解耦

- 采集、识别和监控发送必须通过明确的生产者/消费者队列解耦。
- ASR 队列和监控音频队列必须分开，不能共用一个会被网络耗尽的无限缓冲。
- 所有实时队列必须有容量上限，并记录当前深度、峰值、丢弃数量和溢出事件。
- 监控音频队列满时可以按既定策略丢弃旧数据，但不得静默丢弃识别链路数据；识别链路过载时必须有明确的恢复策略和错误状态。
- 禁止使用无限增长的容器保存实时音频、事件或历史记录。

### 线程与资源生命周期

- 禁止分离线程。所有线程必须支持停止、回收和异常退出。
- ALSA、网络和队列等待必须支持超时、取消或停止信号，禁止永久阻塞和忙等循环。
- 收到 `SIGINT` 或 `SIGTERM` 后，必须按顺序停止生产者、停止消费者、释放 ALSA/网络/模型资源并 join 所有线程。
- 单个线程或组件的异常不得导致其他核心组件无提示地退出。

### 网络与重连

- 网络断开、主机未启动、主机重启和浏览器关闭都必须被视为可恢复场景。
- 重连必须使用退避策略，禁止无限快速重试。
- 网络不可用时，板端不得无限缓存监控音频。
- 网络错误、重连次数、最后一次成功发送时间和当前连接状态必须可观测。

### ALSA 与音频参数

- 不得假设所有 RK3588 板子的 3.5mm 输入设备名相同；设备名和音频参数必须可配置或通过设备检测获得。
- 采样率、声道数、PCM 格式、Period 和 Buffer 参数必须在日志或状态中明确显示。
- 必须处理 ALSA 打开失败、读取失败、设备短暂异常和 XRUN，并区分可恢复与不可恢复错误。
- 默认语音参数可以使用 16 kHz、单声道、`S16_LE`，但必须以实际设备能力为准。

### VAD 与离线 ASR

- 第一阶段 ASR 必须本地运行，不依赖云 API。
- VAD 的开始、持续、结束状态和 ASR 的开始、完成、失败状态必须有明确含义。
- 模型路径、模型版本、运行后端和语言配置不得隐式硬编码。
- 模型加载失败、识别超时或识别异常必须上报状态并记录原因，不能静默吞错。

### 通信协议

- WebSocket 地址、端口、路径、心跳、超时和重连规则必须可配置。
- 音频消息必须明确编码、采样率、声道、分片大小、时间戳和序列号。
- 状态和识别结果必须定义稳定的 JSON 字段、消息类型和示例。
- 修改消息格式、音频格式或端口时，必须同步更新协议文档、示例和测试；必要时增加协议版本号。

### 可观测性与隐私

- 日志必须包含时间、组件、级别和可定位的错误原因；重复错误需要限频，不能刷屏。
- 至少提供音频设备状态、VAD/ASR 状态、识别耗时、网络状态、队列状态、丢弃统计和运行时间。
- 音频和识别文本属于敏感数据。默认不得上传云端，也不得在日志中持续输出原始音频或完整敏感文本。
- 密钥、口令和真实部署地址不得写入源码或提交到仓库。

## 第一阶段范围

除非用户明确要求，不实现以下功能：

- TTS、AEC、AGC、降噪、Beamforming
- 多麦克风阵列
- 大模型对话、Qwen/LLM
- 摄像头、视频推流和 RTMP 音视频合流
- 数据库、用户系统、复杂权限、云服务和 OTA

不得为了“完善架构”提前引入与当前 Phase 无关的组件或依赖。

## Agent 工作流程

1. 先确认当前 Phase、目标、输入输出和验收条件。
2. 先检查现有文件和用户未提交的修改，不覆盖或清理无关改动。
3. 一次只完成一个明确功能，避免同时实现全部 Phase。
4. 修改公共协议、线程模型、队列策略或目录结构前，说明影响范围并同步文档。
5. 优先使用最小、可测试的实现；不要引入没有实际用途的抽象和依赖。
6. 遇到硬件或模型不可用时，提供可复现的模拟、冒烟或主机侧测试方式，不得伪造硬件测试结果。

## 验证与交付

- 每次改动后至少执行与改动相关的构建、单元测试、协议测试或冒烟测试。
- 涉及并发、网络或音频的改动，需要覆盖停止、断线、重连、队列满和异常路径。
- 稳定性结论必须说明测试时长、硬件/系统环境和观察指标；不能只写“测试通过”。
- 完成任务时必须报告：改动内容、验证命令及结果、未完成项、已知限制和后续风险。
- 不得删除数据、重置仓库或覆盖用户文件，除非用户明确授权。
